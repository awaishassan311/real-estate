PORT=5000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=real_estate_backend
DB_USER=postgres
DB_PASSWORD=

JWT_SECRET=replace-with-a-long-random-secret

# Set to true only when you intentionally want Sequelize to sync models on startup.
DB_SYNC=false
